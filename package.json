{
  "name": "menu",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "ui5 serve --open",
    "build": "ui5 build self-contained --all --clean-dest --dest docs"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@ui5/cli": "^2.14.0"
  }
}
